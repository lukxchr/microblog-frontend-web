query Posts($limit: Int!, $cursor: String) {
  posts(limit: $limit, cursor: $cursor) {
    hasMore
    posts {
      id
      createdAt
      updatedAt
      textSnippet
      creatorId
      likes
      creator {
        id
        username
        email
        createdAt
        updatedAt
      }
    }
  }
}
